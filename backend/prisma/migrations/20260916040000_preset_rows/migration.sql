-- 역할 프리셋 기본 구성을 행으로 적재한다(2026-09-16) — 이제 기능모듈 화면에서 자리마다 켜고 끌 수 있다.
-- 코드(shared/rbac/presets.ts)의 값을 그대로 옮긴 것이고, 이 행이 하나도 없으면 코드 기본값이 쓰인다.
INSERT INTO "access_control" ("subject_type", "subject_ref", "module_code", "enabled", "memo") VALUES
  ('preset', 'sales_mgr', 'addon.view', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'sales_mgr', 'customer.view', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'sales_mgr', 'doc.send.email', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'sales_mgr', 'doc.send.sign', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'sales_mgr', 'doc.view', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'sales_mgr', 'notify.push', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'sales_mgr', 'order.confirm', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'sales_mgr', 'order.view', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'sales_mgr', 'quote.confirm', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'sales_mgr', 'quote.edit', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'sales_mgr', 'stats.all', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'sales_mgr', 'stats.own', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'pm', 'addon.manage', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'pm', 'basedata.dims', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'pm', 'basedata.makerprice', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'pm', 'basedata.optiondb', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'pm', 'basedata.weights', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'pm', 'checklist.manage', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'pm', 'doc.view', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'pm', 'notify.push', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'pm', 'order.confirm', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'pm', 'order.control', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'pm', 'order.remove', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'pm', 'order.view', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'pm', 'quote.confirm', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'pm', 'quote.edit', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'pm', 'stats.all', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'pm', 'stats.own', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'prod_mgr', 'addon.manage', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'prod_mgr', 'basedata.makerprice', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'prod_mgr', 'checklist.manage', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'prod_mgr', 'doc.view', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'prod_mgr', 'notify.push', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'prod_mgr', 'order.confirm', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'prod_mgr', 'order.control', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'prod_mgr', 'order.view', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'prod_mgr', 'quote.confirm', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'prod_mgr', 'quote.edit', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'prod_mgr', 'stats.all', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'prod_mgr', 'stats.own', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'exec', 'addon.view', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'exec', 'doc.view', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'exec', 'notify.push', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'exec', 'order.view', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'exec', 'quote.confirm', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'exec', 'quote.edit', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'exec', 'stats.all', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'exec', 'stats.own', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'master', 'account.manage', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'master', 'addon.manage', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'master', 'addon.view', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'master', 'basedata.dims', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'master', 'basedata.holiday', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'master', 'basedata.makerprice', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'master', 'basedata.manage', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'master', 'basedata.optiondb', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'master', 'basedata.weights', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'master', 'checklist.manage', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'master', 'customer.view', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'master', 'doc.send.email', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'master', 'doc.send.sign', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'master', 'doc.view', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'master', 'notify.assign', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'master', 'notify.push', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'master', 'order.confirm', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'master', 'order.control', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'master', 'order.remove', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'master', 'order.view', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'master', 'quote.confirm', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'master', 'quote.create', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'master', 'quote.delete', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'master', 'quote.edit', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'master', 'stats.all', true, '프리셋 기본값 — 기능모듈 화면에서 고친다'),
  ('preset', 'master', 'stats.own', true, '프리셋 기본값 — 기능모듈 화면에서 고친다')
ON CONFLICT ("subject_type", "subject_ref", "module_code") DO NOTHING;
