-- 손익 — 자동 기입 칸도 사람이 고칠 수 있게 됐다(2026-09-17 지시).
-- 공급가액·계약금은 이미 칸이 있다. VAT 는 **저장하지 않는 계산값**이라, 고친 값을 담을 칸만 더한다.
-- 비어 있으면(null) 예전처럼 엑셀 식(공급가액 × 10% 버림)을 쓴다. 기존 줄은 모두 null 로 시작한다.
-- ⚠️ 칸을 더하기만 한다 — 기존 칸·줄은 건드리지 않는다.
ALTER TABLE "order_pnl" ADD COLUMN IF NOT EXISTS "vat_override" BIGINT;
