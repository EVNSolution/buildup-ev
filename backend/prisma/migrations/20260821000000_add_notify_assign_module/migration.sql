-- 제작 배정 알림 메일을 **받을 사람을 고를 수 있게** 하는 기능모듈.
--
-- 예전에는 전자서명이 완료되면 **활성 관리자 전원**에게 메일이 나갔다. 관리자 권한만
-- 있으면 배정과 무관한 사람에게도 계속 갔다. 이제 다른 기능처럼 계정별 토글로 고른다.
--
-- ⚠️ 참조 데이터 한 줄을 더하는 것뿐이다. 기존 행·컬럼을 건드리지 않는다.
--    이미 있으면 아무 일도 하지 않는다(재실행해도 안전).
--
-- ⚠️ 역할 기본값(access_control)은 만들지 않는다 — **기본은 아무도 안 받는 것**이다.
--    배포 후 관리자 화면에서 받을 사람을 켜야 메일이 나간다.
INSERT INTO "feature_module" ("code", "name", "surface", "sort_order", "active")
VALUES ('notify.assign', '제작 배정 알림 메일', '관리자', 10, true)
ON CONFLICT ("code") DO NOTHING;
