-- 계약에 단가가 없는 항목도 **분류는 해 둔다.**
--
-- 「특장사가 하는 일인데 단가가 아직 없다」와 「EV& 가 직접 해서 발주서에 안 실린다」는
-- 다른 말이다. 앞은 발주서에 **금액만 빈 칸으로** 뜨고, 뒤는 아예 안 뜬다.
-- 0 으로 채워 두면 「무상으로 해 주기로 했다」가 되어 버린다.
--
-- ⚠️ NULL 을 허용하는 쪽으로만 넓힌다 — 있던 값은 그대로다(CLAUDE.md: 더하기만).
ALTER TABLE "maker_price" ALTER COLUMN "unit_price" DROP NOT NULL;
