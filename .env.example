# .env.example — 실제 값은 .env 에 입력 (절대 커밋 금지)

# 서버
PORT=3001
NODE_ENV=development

# 데이터베이스 (TODO: DBMS 확정 후 연결)
DATABASE_URL=

# 인증/JWT (TODO: 인증 실제구현)
JWT_SECRET=
JWT_EXPIRES_IN=7d

# CORS
CORS_ORIGIN=http://localhost:5173

# 외부 API (미확정)
VIVAR_API_URL=
VIVAR_API_KEY=
