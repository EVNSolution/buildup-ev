-- 영업 수락 시각 — 관리자가 배정한 공개 문의를 담당 영업이 **받겠다고 누른** 시점.
--
-- 배정만으로 담당이 정해졌다고 보면, 영업이 아직 보지도 않은 문의가 「내 견적」에 섞여
-- 들어와 누가 붙었는지 아무도 모르는 상태가 된다. 특장사가 주문을 수락하는 것과 같은 이유다.
--
-- NULL = 아직 수락하지 않음(배정 전이거나, 배정됐지만 수락 대기).
-- 재배정하면 다시 NULL 로 돌린다(새 담당이 다시 받아야 한다).
ALTER TABLE "quote" ADD COLUMN IF NOT EXISTS "sales_accepted_at" TIMESTAMP(3);
