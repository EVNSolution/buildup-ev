-- 옛 주문 6단계 문자열을 걷어낸다.
--
-- `order.status` 는 「제작착수→구조변경→튜닝신청→안전검사→튜닝승인→인도완료」 한 줄이었다.
-- 그 순서는 확정된 적이 없고(2026-08-14 회의), 차량·특장이 따로 도는 실제 흐름을 담지 못한다.
-- 진행은 이제 order_step 이 갖는다.
--
-- ⚠️ 되돌릴 수 없다. 실행 전 pg_dump 백업.
--    코드에서 이 컬럼을 읽는 곳은 모두 제거했다(라우트·화면·타입).
--    남아 있던 값은 이관 근거로도 쓰지 않았다 — 확정되지 않은 값이라 사실로 취급하지 않았다.
ALTER TABLE "order" DROP COLUMN IF EXISTS "status";
